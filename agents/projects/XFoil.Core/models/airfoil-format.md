# AirfoilFormat

- File: `src-cs/XFoil.Core/Models/AirfoilFormat.cs`
- Kind: enum
- Values: source/interpretation markers for parsed/generated geometry.

## Notes

- Used to preserve input provenance through downstream workflows.

## Parity

- Smaller than legacy format handling in `aread.f`.

## TODO

- Document any legacy format variants that still need explicit mapping into enum values.
