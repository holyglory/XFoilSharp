---
phase: 03-viscous-solver-parity-and-polar-validation
plan: 18
type: execute
wave: 2
depends_on: [03-17]
files_modified:
  - tests/XFoil.Core.Tests/ViscousParityTests.cs
  - tests/XFoil.Core.Tests/PolarParityTests.cs
  - tests/XFoil.Core.Tests/ViscousSolverEngineTests.cs
  - tests/XFoil.Core.Tests/PolarSweepRunnerTests.cs
autonomous: true
requirements: [VISC-05, POL-01, POL-02, POL-03]
gap_closure: true

must_haves:
  truths:
    - "Newton solver converges in <=20 iterations (not 200)"
    - "ViscousParityTests pass with CL and CD relative tolerance of 1e-5 (0.001%)"
    - "PolarParityTests pass with CL and CD relative tolerance of 1e-5 (0.001%)"
    - "All convergence assertions require strict Converged==true (no iteration-sufficient fallback)"
  artifacts:
    - path: "tests/XFoil.Core.Tests/ViscousParityTests.cs"
      provides: "Single-point parity tests at 0.001% tolerance"
      contains: "1e-5"
    - path: "tests/XFoil.Core.Tests/PolarParityTests.cs"
      provides: "Polar sweep parity tests at 0.001% tolerance"
      contains: "1e-5"
  key_links:
    - from: "ViscousParityTests"
      to: "ViscousSolverEngine"
      via: "End-to-end viscous solve with full Jacobians from plan 03-17"
      pattern: "Assert.*Converged.*true"
---

<objective>
Tighten all parity test tolerances to 1e-5 (0.001%) and restore strict convergence assertions after the Jacobian rewrite in plan 03-17 enables proper Newton convergence.

Purpose: With full chain-rule Jacobians, the Newton solver should converge in 5-20 iterations to the same fixed point as Fortran XFoil. This plan validates that by tightening tolerances from 22%/90% down to 0.001% and requiring strict convergence.

Output: All parity tests passing at 0.001% tolerance with strict convergence
</objective>

<execution_context>
@/home/holyglory/.claude/get-shit-done/workflows/execute-plan.md
@/home/holyglory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-viscous-solver-parity-and-polar-validation/03-17-SUMMARY.md

@tests/XFoil.Core.Tests/ViscousParityTests.cs
@tests/XFoil.Core.Tests/PolarParityTests.cs
@tests/XFoil.Core.Tests/ViscousSolverEngineTests.cs
@tests/XFoil.Core.Tests/PolarSweepRunnerTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten single-point and engine test tolerances and convergence assertions</name>
  <files>tests/XFoil.Core.Tests/ViscousParityTests.cs, tests/XFoil.Core.Tests/ViscousSolverEngineTests.cs</files>
  <action>
In ViscousParityTests.cs:
1. Change `CL_RelativeTolerance` from 0.22 to 1e-5
2. Change `CD_RelativeTolerance` from 0.90 to 1e-5
3. Change `CM_AbsoluteTolerance` from 0.15 to 1e-5
4. Replace ALL `Assert.True(result.Converged || result.Iterations >= 50, ...)` with `Assert.True(result.Converged, "Viscous solver must converge")`
5. Reduce `maxViscousIterations` from 200 back to 50 (Fortran converges in ~10)
6. Update class-level docstring to remove "RESIDUAL GAP" language -- replace with "Validated at 0.001% parity with Fortran XFoil"

In ViscousSolverEngineTests.cs:
1. Replace all `Assert.True(result.Converged || result.Iterations >= 50, ...)` with `Assert.True(result.Converged, ...)`
2. Reduce `maxIterations` from 200 back to 50

**IMPORTANT:** If any tests fail after tightening, this indicates the Jacobian rewrite in 03-17 did not fully resolve the accuracy gap. In that case:
- Run the failing test individually to get the actual vs expected values
- Document the actual accuracy achieved (e.g., "CL within 0.5%, CD within 2%")
- Set tolerances to 2x the measured error (to allow headroom) rather than leaving at 22%/90%
- Add a comment noting the remaining gap for future investigation
- Do NOT leave tolerances at the old 22%/90% values -- tighten to whatever the new solver achieves

This is an iterative approach: tighten to 1e-5 first, then back off to measured accuracy if needed.
  </action>
  <verify>
    <automated>cd /home/holyglory/agents/XFoilSharp && dotnet test tests/XFoil.Core.Tests/ --filter "FullyQualifiedName~ViscousParity|FullyQualifiedName~ViscousSolverEngine" --no-restore -v q 2>&1 | tail -15</automated>
  </verify>
  <done>ViscousParityTests pass with tolerances at 1e-5 (or tightest achievable). ViscousSolverEngineTests require strict convergence. No iteration-sufficient fallbacks remain.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten polar sweep test tolerances and convergence assertions</name>
  <files>tests/XFoil.Core.Tests/PolarParityTests.cs, tests/XFoil.Core.Tests/PolarSweepRunnerTests.cs</files>
  <action>
In PolarParityTests.cs:
1. Change `CL_RelativeTolerance` from 0.48 to 1e-5
2. Change `CD_RelativeTolerance` from 0.90 to 1e-5
3. Change `CM_AbsoluteTolerance` from 0.15 to 1e-5
4. Update class-level docstring to remove "RESIDUAL GAP" language
5. Restore strict convergence filtering (converged points only, no non-converged fallback)

In PolarSweepRunnerTests.cs:
1. Replace convergence-lenient assertions with strict `Assert.True(result.Converged, ...)`
2. Reduce `maxIterations` from 200 back to 50

**Same iterative approach as Task 1:** If 1e-5 fails, measure actual accuracy and set tolerances to 2x measured error. Document any remaining gap.

After all changes, run the full test suite to verify no regressions:
```
dotnet test tests/XFoil.Core.Tests/ --no-restore -v q
```
  </action>
  <verify>
    <automated>cd /home/holyglory/agents/XFoilSharp && dotnet test tests/XFoil.Core.Tests/ --no-restore -v q 2>&1 | tail -15</automated>
  </verify>
  <done>All 309+ tests pass. PolarParityTests at 1e-5 tolerance (or tightest achievable). All convergence assertions are strict. No iteration-sufficient fallbacks anywhere in test suite. Class docstrings updated to reflect achieved accuracy.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `dotnet test tests/XFoil.Core.Tests/ --no-restore` -- ALL tests pass
2. grep for "0.22\|0.48\|0.90\|Iterations >= 50\|Iterations >= 200" in test files returns NO matches
3. grep for "1e-5\|0.00001" in ViscousParityTests.cs and PolarParityTests.cs returns matches
4. grep for "RESIDUAL GAP" in test files returns NO matches
</verification>

<success_criteria>
- ViscousParityTests: CL, CD, CM within 1e-5 of Fortran XFoil reference values (or documented tightest achievable)
- PolarParityTests: All 3 sweep types within 1e-5 (or documented tightest achievable)
- All convergence assertions strict (Converged == true)
- maxViscousIterations back to 50
- Full test suite green (309+ tests)
- VISC-05, POL-01, POL-02, POL-03 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-viscous-solver-parity-and-polar-validation/03-18-SUMMARY.md`
</output>
