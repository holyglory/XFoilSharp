---
phase: 01-foundation-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Directory.Build.props
  - src/XFoil.Core/XFoil.Core.csproj
  - src/XFoil.Solver/XFoil.Solver.csproj
  - src/XFoil.Design/XFoil.Design.csproj
  - src/XFoil.IO/XFoil.IO.csproj
  - src/XFoil.Cli/XFoil.Cli.csproj
  - tests/XFoil.Core.Tests/XFoil.Core.Tests.csproj
autonomous: true
requirements:
  - FW-01
  - FW-02

must_haves:
  truths:
    - "dotnet build src/XFoil.CSharp.slnx succeeds with zero errors and zero warnings"
    - "dotnet test tests/XFoil.Core.Tests/ runs without framework errors"
    - "All 6 projects target net10.0 (via centralized Directory.Build.props)"
    - "No individual csproj contains a TargetFramework element (centralized in Directory.Build.props)"
  artifacts:
    - path: "Directory.Build.props"
      provides: "Centralized TargetFramework net10.0"
      contains: "<TargetFramework>net10.0</TargetFramework>"
    - path: "src/XFoil.Core/XFoil.Core.csproj"
      provides: "Cleaned csproj with properties centralized"
    - path: "tests/XFoil.Core.Tests/XFoil.Core.Tests.csproj"
      provides: "Updated test packages for .NET 10 compatibility"
      contains: "xunit"
  key_links:
    - from: "Directory.Build.props"
      to: "src/*/*.csproj"
      via: "MSBuild property inheritance"
      pattern: "<TargetFramework>net10.0</TargetFramework>"
    - from: "tests/XFoil.Core.Tests/XFoil.Core.Tests.csproj"
      to: "src/*/*.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference Include=\"\\.\\.\\\\.\\.\\.\\\\src\\\\"
---

<objective>
Upgrade the entire solution from .NET 8 to .NET 10, centralize TargetFramework in Directory.Build.props, update test packages, and verify zero-warning build.

Purpose: FW-01 requires all projects target net10.0. FW-02 requires the solution builds cleanly with zero warnings. The research identified that centralizing TargetFramework is consistent with existing Directory.Build.props patterns (already centralizes LangVersion, Nullable, ImplicitUsings, TreatWarningsAsErrors). Test packages need updating for .NET 10 compatibility.

Output: All 6 projects targeting net10.0 via centralized Directory.Build.props, updated test packages, clean solution build
</objective>

<execution_context>
@/home/holyglory/.claude/get-shit-done/workflows/execute-plan.md
@/home/holyglory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-cleanup/01-RESEARCH.md
@.planning/phases/01-foundation-cleanup/01-01-SUMMARY.md
@Directory.Build.props
@src/XFoil.Core/XFoil.Core.csproj
@src/XFoil.Solver/XFoil.Solver.csproj
@src/XFoil.Design/XFoil.Design.csproj
@src/XFoil.IO/XFoil.IO.csproj
@src/XFoil.Cli/XFoil.Cli.csproj
@tests/XFoil.Core.Tests/XFoil.Core.Tests.csproj

<interfaces>
<!-- Current Directory.Build.props (centralized build properties) -->
```xml
<Project>
  <PropertyGroup>
    <LangVersion>10.0</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>
</Project>
```

<!-- Typical library csproj (e.g., XFoil.Core) has these redundant properties to remove: -->
```xml
<PropertyGroup>
  <TargetFramework>net8.0</TargetFramework>
  <ImplicitUsings>enable</ImplicitUsings>
  <Nullable>enable</Nullable>
</PropertyGroup>
```

<!-- XFoil.Cli csproj has OutputType plus redundant properties: -->
```xml
<PropertyGroup>
  <OutputType>Exe</OutputType>
  <TargetFramework>net8.0</TargetFramework>
  <ImplicitUsings>enable</ImplicitUsings>
  <Nullable>enable</Nullable>
</PropertyGroup>
```

<!-- Test csproj has these test-specific properties plus redundant ones: -->
```xml
<PropertyGroup>
  <TargetFramework>net8.0</TargetFramework>
  <ImplicitUsings>enable</ImplicitUsings>
  <Nullable>enable</Nullable>
  <IsPackable>false</IsPackable>
  <IsTestProject>true</IsTestProject>
</PropertyGroup>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize TargetFramework and clean up csproj files</name>
  <files>Directory.Build.props, src/XFoil.Core/XFoil.Core.csproj, src/XFoil.Solver/XFoil.Solver.csproj, src/XFoil.Design/XFoil.Design.csproj, src/XFoil.IO/XFoil.IO.csproj, src/XFoil.Cli/XFoil.Cli.csproj, tests/XFoil.Core.Tests/XFoil.Core.Tests.csproj</files>
  <action>
1. Update Directory.Build.props to add `<TargetFramework>net10.0</TargetFramework>` as the FIRST property in the PropertyGroup (before LangVersion). The final file should be:
```xml
<Project>
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>10.0</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>
</Project>
```

2. Clean up each of the 5 src/ csproj files by removing properties now centralized in Directory.Build.props: `<TargetFramework>`, `<ImplicitUsings>`, `<Nullable>`. Keep any project-specific properties (like `<OutputType>Exe</OutputType>` in XFoil.Cli). For library projects with NO remaining properties or items, the result should be a minimal csproj like:
```xml
<Project Sdk="Microsoft.NET.Sdk">
</Project>
```
For XFoil.Cli, keep the OutputType and ProjectReferences:
```xml
<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <ProjectReference Include="..\XFoil.Core\XFoil.Core.csproj" />
    ...
  </ItemGroup>
  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>
</Project>
```

3. Clean up tests/XFoil.Core.Tests/XFoil.Core.Tests.csproj by removing `<TargetFramework>`, `<ImplicitUsings>`, `<Nullable>`. Keep `<IsPackable>false</IsPackable>` and `<IsTestProject>true</IsTestProject>`.

4. Update test package versions in the test csproj:
   - `xunit` from 2.5.3 to 2.9.3
   - `xunit.runner.visualstudio` from 2.5.3 to 2.8.2
   - `Microsoft.NET.Test.Sdk` from 17.8.0 to 18.3.0
   - `coverlet.collector` keep at 6.0.0 (or update to latest 6.x if available)

5. Do NOT change LangVersion -- keep it pinned at 10.0 per research recommendation (C# 14 introduces Span overload resolution changes that could affect the codebase).

6. After all edits, verify no csproj file still contains `<TargetFramework>` (it should only be in Directory.Build.props):
   `grep -r '<TargetFramework>' --include='*.csproj' .`
   This MUST return zero results.
  </action>
  <verify>
    <automated>cd /home/holyglory/agents/XFoilSharp && grep -r '<TargetFramework>' --include='*.csproj' . | wc -l | xargs test 0 -eq && grep '<TargetFramework>net10.0</TargetFramework>' Directory.Build.props > /dev/null && echo "PASS: TFM centralized to net10.0"</automated>
  </verify>
  <done>Directory.Build.props contains TargetFramework net10.0. No individual csproj contains TargetFramework. No csproj contains redundant ImplicitUsings or Nullable (already in Directory.Build.props). Test packages updated to .NET 10 compatible versions.</done>
</task>

<task type="auto">
  <name>Task 2: Build solution and verify zero warnings</name>
  <files>(no file changes -- verification only)</files>
  <action>
1. Run `dotnet restore src/XFoil.CSharp.slnx` to restore all packages including updated test packages. If NuGet audit warnings appear (NU1903/NU1904 from .NET 10's new transitive audit default), add `<NuGetAuditMode>direct</NuGetAuditMode>` to Directory.Build.props and re-restore.

2. Run `dotnet build src/XFoil.CSharp.slnx --no-restore` and verify:
   - Zero errors
   - Zero warnings (TreatWarningsAsErrors is already enabled, so any warning would be an error)
   - All 6 projects build successfully

3. If any new warnings/errors appear due to .NET 10 SDK analyzer changes:
   - Read the error messages carefully
   - Fix the underlying code issues (preferred) or suppress specific analyzer warnings if they are false positives
   - Do NOT disable TreatWarningsAsErrors

4. Run `dotnet test tests/XFoil.Core.Tests/ --no-build` to verify the test project runs. If tests fail due to framework issues (not logic), investigate and fix. Note: test logic failures are expected to exist and are NOT blockers for this plan -- only framework/runtime errors matter.

5. Final verification: `dotnet build src/XFoil.CSharp.slnx` (full build from clean) must complete with 0 warnings, 0 errors.
  </action>
  <verify>
    <automated>cd /home/holyglory/agents/XFoilSharp && dotnet build src/XFoil.CSharp.slnx 2>&1 | tail -5</automated>
  </verify>
  <done>dotnet build src/XFoil.CSharp.slnx succeeds with 0 warnings and 0 errors across all 6 projects targeting net10.0. dotnet test runs without framework errors.</done>
</task>

</tasks>

<verification>
1. `grep '<TargetFramework>net10.0</TargetFramework>' Directory.Build.props` confirms centralized TFM
2. `grep -r '<TargetFramework>' --include='*.csproj' .` returns empty (no individual csproj has TFM)
3. `dotnet build src/XFoil.CSharp.slnx` completes with 0 warnings, 0 errors
4. `dotnet test tests/XFoil.Core.Tests/` runs without framework/runtime errors
5. `grep 'LangVersion' Directory.Build.props` confirms LangVersion still pinned at 10.0
</verification>

<success_criteria>
- Directory.Build.props declares TargetFramework as net10.0
- All 6 csproj files have no TargetFramework, ImplicitUsings, or Nullable elements (centralized)
- Test project packages updated: xunit 2.9.3, xunit.runner.visualstudio 2.8.2, Microsoft.NET.Test.Sdk 18.3.0
- LangVersion remains pinned at 10.0
- `dotnet build src/XFoil.CSharp.slnx` produces 0 errors, 0 warnings
- `dotnet test` executes without framework errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cleanup/01-02-SUMMARY.md`
</output>
